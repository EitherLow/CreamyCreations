@model IEnumerable<CreamyCreations.Models.Cover>

@{
    ViewData["Title"] = "EditCovers";
}

<h1>Edit Covers</h1>

<h4>Covers</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="EditCovers">
            @foreach (Cover cov in Model)
            {
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="@cov.CoverId" />
                <div class="form-group">
                    <label asp-for="@cov.Flavor" class="control-label"></label>
                    <input asp-for="@cov.Flavor" class="form-control" />
                    <span asp-validation-for="@cov.Flavor" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="@cov.Price" class="control-label"></label>
                    <input asp-for="@cov.Price" class="form-control" />
                    <span asp-validation-for="@cov.Price" class="text-danger"></span>
                </div>
                <hr />
            }
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
