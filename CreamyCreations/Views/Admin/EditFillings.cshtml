@model CreamyCreations.ViewModels.FillingsVM

@{
    ViewData["Title"] = "EditFillings";
}

<h1>EditFillings</h1>

<h4>FillingsVM</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="EditFillings">
            @for (var i = 0; i < Model.Fillings.Count(); i++)
            {
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="@Model.Fillings[i].FillingId" />
                <div class="form-group">
                    <label asp-for="@Model.Fillings[i].Flavor" class="control-label"></label>
                    <input asp-for="@Model.Fillings[i].Flavor" class="form-control" />
                    <span asp-validation-for="@Model.Fillings[i].Flavor" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="@Model.Fillings[i].Price" class="control-label"></label>
                    <input asp-for="@Model.Fillings[i].Price" class="form-control" />
                    <span asp-validation-for="@Model.Fillings[i].Price" class="text-danger"></span>
                </div>
                <hr />
            }
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Customization">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
